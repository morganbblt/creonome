NODE_ENV: production
API_PREFIX: api
CORS_ALLOWED_ORIGINS: "https://www.creonome.com,https://creonome.vercel.app,http://localhost:3000"
GOOGLE_CLOUD_PROJECT: creonome
GCP_MEDIA_BUCKET: creonome-909754432431-media
GEMINI_MODEL: gemini-3.6-flash
# Production always takes the Vertex AI branch in ai.module.ts, so this is
# the model version actually in effect — keep it equal to GEMINI_MODEL
# above (see packages/config/src/api-env.ts).
VERTEX_AI_MODEL: gemini-3.6-flash
VIDEO_PROVIDER: auto
VEO_BACKEND: vertex
VEO_VERTEX_LOCATION: us-central1
VEO_VERTEX_MODEL: veo-3.1-fast-generate-001
VEO_GEMINI_MODEL: veo-3.1-fast-generate-preview
VEO_POLL_INTERVAL_MS: "10000"
VEO_TIMEOUT_MS: "240000"
FEATURE_SOCIAL_CONNECTIONS: "false"
DEMO_MODE: "true"
DEFAULT_CREDIT_BALANCE: "60"
